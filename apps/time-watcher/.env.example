export PHONE_NUMBER=5551234567
export PARTY_SIZE=2
export VENUE_URL="https://www.opentable.com/r/your-cool-restaurant-here"

# ── Lookahead range (days from today) ─────────────────────────────────
# Queries availability for each date from today+START through today+END.
# Detects when a date transitions from "no availability" to "has slots"
# (i.e. the restaurant just released that future date for booking).
# LOOKAHEAD_START=11
# LOOKAHEAD_END=15

# ── Polling config (optional) ─────────────────────────────────────────
# Base delay between polls in ms (default: 60000 = 1 min)
# POLL_INTERVAL_MS=60000

# Max random jitter ±ms added to delay (default: 20000 = ±20s)
# POLL_JITTER_MS=20000

# Path for append-only JSONL request/response dump (optional)
# DUMP_FILE=./time-watcher-dump.jsonl

# Stop after N polls (optional, omit to run forever)
# MAX_POLLS=100
